name: Deploy Contracts

# Manually triggered deployment workflow: this is the "smart contract
# deployment workflow" requirement. Run it from the Actions tab, or on
# tags like v1.0.0, to build + deploy all three contracts to a chosen
# Stellar network and print the resulting contract IDs.

on:
  workflow_dispatch:
    inputs:
      network:
        description: "Target network"
        required: true
        default: "testnet"
        type: choice
        options:
          - testnet
          - mainnet
  push:
    tags:
      - "v*.*.*"

jobs:
  deploy:
    name: Build & Deploy Contracts
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32v1-none

      - name: Cache cargo registry & bin
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            ~/.cargo/bin
          key: ${{ runner.os }}-cargo-stellar-cli-v26
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y libdbus-1-dev pkg-config libudev-dev

      - name: Install Stellar CLI
        run: |
          wget https://github.com/stellar/stellar-cli/releases/download/v26.1.0/stellar-cli-26.1.0-x86_64-unknown-linux-gnu.tar.gz
          tar -xvzf stellar-cli-26.1.0-x86_64-unknown-linux-gnu.tar.gz
          sudo mv stellar /usr/local/bin/

      - name: Configure deployer identities
        run: |
          stellar keys generate deployer --network testnet --fund

      - name: Build and deploy contracts
        run: |
          chmod +x scripts/deploy.sh
          ./scripts/deploy.sh
