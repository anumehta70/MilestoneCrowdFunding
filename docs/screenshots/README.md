# Screenshots

Drop the submission checklist screenshots here with these exact names so the
links in `SUBMISSION_CHECKLIST.md` resolve:

- `mobile-home.png` — home/explore page at a phone viewport width (~375px)
- `mobile-campaign.png` — campaign detail page at a phone viewport width
- `ci-pipeline.png` — GitHub Actions tab showing `ci.yml` passing (green checks)
- `test-output-contracts.png` — terminal output of `cargo test` across the 3 contracts
- `test-output-frontend.png` — terminal output of `npm test -- --run`

Use your browser's device toolbar (Chrome DevTools → Toggle device toolbar,
or Firefox Responsive Design Mode) set to an iPhone or Pixel width for the
mobile screenshots.
